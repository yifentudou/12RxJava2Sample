<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.potato.rxjavasample">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TimerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PublishMergeActivity">

        </activity>
        <activity android:name=".CombineLatestActivity"></activity>
        <activity android:name=".RetryActivity" />
        <activity android:name=".PollingActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".BufferActivity" />
        <activity android:name=".BackgroundActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>